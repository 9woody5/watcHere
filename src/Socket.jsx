import { io } from "socket.io-client";

export const URL = `http://kdt-sw-6-team05.elicecoding.com`;
// ws: //localhost:8080/ws
// topicws: topic;

// export const socket = io(URL, {
//   autoConnect: false,
// });

const Socket = () => {
  return <div></div>;
};

export default Socket;
